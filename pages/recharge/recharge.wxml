<view class="recharge-page">
  <view class="header">
    <text class="title">余额充值</text>
    <text class="subtitle">仅小程序支付</text>
  </view>

  <view class="form">
    <view class="form-item">
      <text class="label">充值金额（元）</text>
      <view class="selected-amount" wx:if="{{amount}}">
        <text class="selected-amount-text">已选择：¥{{amount}}</text>
      </view>
      <view class="selected-amount-placeholder" wx:else>
        <text class="placeholder-text">请选择充值金额</text>
      </view>
    </view>
    <view class="quick-amounts">
      <view 
        wx:for="{{rechargeAmounts}}" 
        wx:key="key" 
        class="amount-btn {{amount == item.key ? 'active' : ''}}" 
        bindtap="onQuickPick" 
        data-val="{{item.value}}"
      >
        ¥{{item.display}}
      </view>
    </view>
    <button class="pay-btn" type="primary" loading="{{submitting}}" disabled="{{!canSubmit || submitting}}" bindtap="onPay">立即支付</button>
  </view>

  <view class="tips">
    <text>· 请从上方选择充值金额</text>
    <text>· 支付结果以订单查询为准</text>
  </view>
</view>

<!-- 支付加载倒计时遮罩 -->
<view class="payment-loading-mask" wx:if="{{showPaymentLoading}}">
  <view class="payment-loading-content">
    <view class="loading-spinner"></view>
    <text class="loading-text">确认支付结果... {{paymentLoadingCountdown}}秒</text>
  </view>
</view>


